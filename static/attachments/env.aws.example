# Copy to .env and change these before any non-local deployment.
# docker-compose reads these; .env is gitignored so real secrets never get committed.

# Postgres password (used by the db service and the backend DATABASE_URL)
POSTGRES_PASSWORD=quickbite123

# JWT signing secret — MUST be a long random string in production
SECRET_KEY=quickbite-docker-secret-change-before-production
